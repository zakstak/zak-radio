package application

// releaseIdentity is replaced by the package build. Development builds retain
// an explicit identity so runtime responses are never ambiguous.
var releaseIdentity = "development"
