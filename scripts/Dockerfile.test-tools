FROM golang:1.26.5-alpine

# This disposable test image follows the package set of its pinned Alpine base.
# hadolint ignore=DL3018
RUN apk add --no-cache \
    bash \
    build-base \
    coreutils \
    findutils \
    python3 \
    rsync \
    sqlite \
    tar \
    util-linux
